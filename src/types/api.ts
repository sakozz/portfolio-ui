const apiHost: string = 'http://localhost:3000/api';
export const apiPath = {
  authPath: `${apiHost}/auth`,
  profilesPath: `${apiHost}/profiles`,
  competencesPath: `${apiHost}/competences`,
  blogsPath: `${apiHost}/pages`,
};
